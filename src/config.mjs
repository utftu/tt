

export default {
    // $FOO (?<!\\)\$(\w+)
    // ${FOO} (?<!\\)\${(\w+)}
    // {{FOO}} (?<!\\){{(\w+)}}
    // regexp: /(?<!\\)\$(\w+)|(?<!\\)\${(\w+)}|(?<!\\){{(\w+)}}/gi,
    regexps: {
        shell: /(?<!\\)\$(\w+)/gi,
        js: /(?<!\\)\${(\w+)}/gi,
        uni: /(?<!\\){{(\w+)}}/gi,
        complex: /(?<!\\)\$(\w+)|(?<!\\)\${(\w+)}|(?<!\\){{(\w+)}}/gi
    }
}